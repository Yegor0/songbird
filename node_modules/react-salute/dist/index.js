import React from 'react';

var ReactSalute = function ReactSalute(_ref) {
  var name = _ref.name;
  return React.createElement("h1", null, "Salute ", name, "!");
};

ReactSalute.defaultProps = {
  name: 'React'
};
export default ReactSalute;